{% extends 'base.html.twig' %}

{% block title %}Brand{% endblock %}

{% block body %}
    <h1>Brand</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ brand.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ brand.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary mb-3" href="{{ path('app_admin_brand_index') }}">back to list</a>

    <a class="btn btn-secondary mb-3" href="{{ path('app_admin_brand_edit', {'id': brand.id}) }}">edit</a>

    {{ include('admin/brand/_delete_form.html.twig') }}
{% endblock %}
