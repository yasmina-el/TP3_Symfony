{% extends 'base.html.twig' %}

{% block title %}Pen{% endblock %}

{% block body %}
    <h1>Pen</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pen.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pen.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ pen.price }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ pen.description }}</td>
            </tr>
            <tr>
                <th>Ref</th>
                <td>{{ pen.ref }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary mb-3" href="{{ path('app_admin_pen_index') }}">back to list</a>

    <a class="btn btn-secondary mb-3" href="{{ path('app_admin_pen_edit', {'id': pen.id}) }}">edit</a>

    {{ include('admin/pen/_delete_form.html.twig') }}
{% endblock %}
