{% extends 'base.html.twig' %}

{% block title %}Color{% endblock %}

{% block body %}
    <h1>Color</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ color.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ color.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary mb-3" href="{{ path('app_admin_color_index') }}">back to list</a>

    <a class="btn btn-secondary mb-3" href="{{ path('app_admin_color_edit', {'id': color.id}) }}">edit</a>

    {{ include('admin/color/_delete_form.html.twig') }}
{% endblock %}
