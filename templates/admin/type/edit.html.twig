{% extends 'base.html.twig' %}

{% block title %}Edit Type{% endblock %}

{% block body %}
    <h1>Edit Type</h1>

    {{ include('admin/type/_form.html.twig', {'button_label': 'Update'}) }}
    <div class="d-flex flex-row">
    <a class="btn btn-outline-primary" href="{{ path('app_admin_type_index') }}">back to list</a>

    {{ include('admin/type/_delete_form.html.twig') }}
    </div>
{% endblock %}
